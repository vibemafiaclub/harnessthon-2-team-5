# C단계 판정 — 2026-09-07 캡처 (shots/index.md: 2026-09-07 02:00 UTC)

기준: `c_checks.md`. design.md §4C 는 RULE-02 하나만 확정이라 나머지는 c_checks 기본 수치를 썼다.

## 화면별 1차 목적 (design.md §5·§7)
| 화면 | 1차 목적 | 목적 통과 |
|---|---|---|
| 00 온보딩 | 입력형(상대 초대) | PASS (히어로 없음) |
| 01 홈 | 현황 파악형(주말 확정+겹침) | **FAIL** F-7 |
| 02 모임 상세 | 현황 파악형(회신 n/N+마감) | PASS |
| 03 지인 회신 | 입력형(가능 날짜 고르기) | **FAIL** F-1 |
| 04 지인 풀 | 현황 파악형(안 묶인 사람 수) | **FAIL** F-6 |
| 05 지인 편집 | 입력형 | PASS |
| 06 모임 편성 | 선택형 | PASS |
| 07 날짜 후보 | 선택형 | PASS |

## FAIL (9)
- **F-1 `local` 03_answering: primary CTA 가 프레임 밖으로 잘림.** 초록 CTA(#0D7A64)가 y=820~843(프레임 844px 마지막 행)까지만 보이고 가시 높이 24px, 라벨 "회신 보내기" 글자 0px. 03_empty 의 같은 버튼은 y≤835 에서 끝난다. → C-6 상태 변화 시 잘림.
- **F-2 `local` 01_normal: 리스트 마지막 행이 CTA 에 글자 중간에서 잘림.** "신랑측 대학 서배" 행 잉크 y=694~704, 불투명 CTA 바 상단 y=705. 스크림·페이드·하단 여백 0. 같은 행 문구 "서배"는 "선배" 오타(C-9 인접).
- **F-3 `local` 05_edit: 옵션 라벨 중복.** "누구 쪽 지인인가" 칩이 `신부 쪽 / 신부 쪽 / 양가 공동`. 05_new·05_oneonone 은 `신부 쪽 / 신랑 쪽 / 양가 공동` 이라 2번째 칩이 오기이고, 하필 그 칩이 선택 상태다.
- **F-4 `local` 선택 상태 표현 3종 혼재(C-8).** ① 05_oneonone 관계 칩=연초록 배경+초록 테두리("양가어른"), 같은 줄 "1:1 필요"=흰 배경+빨강 테두리 ② 같은 화면 "양가 공동"=무채 회색 채움 → 선택이 아니라 비활성으로 읽힘(07 의 실제 비활성과 같은 회색) ③ 06 체크박스=초록 사각형에 체크마크 없음 / 07_selected=초록 원+흰 체크마크.
- **F-5 `local` 상태 배지가 색만 다르고 아이콘이 없다(C-8).** 01_normal 우측 `확정임박`(주황)·`대기`(회색)·`완료`(연녹), 02 상단 `대기`·`확정임박`·`확정` 이 모두 동일 pill 형태·동일 글자 크기, 아이콘 0개. 형태가 같아 색을 못 읽으면 대기/완료가 구분되지 않는다.
- **F-6 `direction` 04_normal: 1등 정보가 화면에서 가장 약하다.** design.md §5 의 1등 정보("안 묶인 사람 수")가 "전체 58명 · 17명은 아직 모임에 안 묶였어요" 본문 1줄(제목 "지인"의 약 1/2 크기)이고, 행 우측 "아직 안 묶임"은 부차 정보 "모임 2건"과 같은 회색·같은 크기·같은 위치라 분리 불가. → 매력판정 "핵심 수치 2배 이상" 실패 + C-2 "위계는 최소 두 가지"(색 하나만) 실패.
- **F-7 `direction` 01_normal: 현황 파악형 실패 조건 적중.** 회신 12/15·8/10·5/9 가 전부 같은 크기 회색 텍스트로만 나열, 진행 막대·비율 0개(같은 데이터를 02_normal 은 초록 막대 3개로 표시하므로 컴포넌트는 이미 있다). 3초 시선이 처음 닿는 곳도 1등 정보가 아니라 폭 358px 초록 CTA "모임 만들기".
- **F-8 `local` 00_connected: 하단 61.4% 공백.** 마지막 잉크 y=325, 이후 518/844px 완전 공백. C-4 "60% 이상 빈 여백+콘텐츠 위쪽 편중" 초과. (00_start 55.6%, 00_invite 54.9% 는 기준 미만이라 PASS)
- **F-9 `local` 비활성 CTA 표현 2종.** 06_none/06_noname=회색 채움+진회색 글자, 07_empty/07_sent=흰 채움+연회색 테두리+연회색 글자.

## PASS 로 확인한 것
- **C-1** 배경 #F8F9F8 / 카드 #FFFFFF 2단계. 유일한 hue 차(120° vs 170.8°)는 채도 7.7%·RGB 1단계 차라 유의미한 색상 아님 → 통과.
- **C-3** 05_new 잉크 기준 라벨→입력 25px, 입력→다음 그룹 26px. "크거나 같으면 실패" 문자 기준 통과(단 차이 1px 로 그룹 경계가 시각적으로는 거의 안 보인다).
- **C-5** 체크 항목 화면당 최대 1개(점선 테두리만: 01_empty·04_empty·05 "+새 그룹"·07 "+다른 날짜 추가"). 그라디언트·glassmorphism·이모지·다크모드·좌측 컬러보더·채움용 통계 0건 → 2개 미만이라 통과.
- **C-6** 빈 상태 4종 모두 다음 행동 버튼 보유(04_empty 는 2개), 오류(06_noname)는 원인+해법 둘 다, 로딩 스켈레톤 2종(01·04) 존재.
- **C-8 순수 검정** 최암부 텍스트 rgb(23,27,24) — #000 사용 0건.
- **RULE-02(design.md §4C)** 브랜드색 #0D7A64 = HSL(167.9, 80.7%, 26.5%) 고채도, 파스텔 아님. 단색 지배 아님(주황 #FFF3DC 동반).
- **매력판정** 이 서비스에만 있는 장치 3건 실측: 06 "동네친구 모임 후보에도 올라 있어요", 07_selected "이미 2건 잡힌 날이에요", 01 "같은 날 두 모임이 겹쳐요". 표면 3단계(배경/카드/강조) 확보. 웨딩 클리셰(핑크·꽃·세리프) 0건.
- 관찰(FAIL 아님): 04 FAB 은 "+" 아이콘만이라 C-8 문자상 걸리나 탭바 아이콘은 전부 라벨 동반이라 단독 미고지.

## 3축 자체 채점
- **UI 심미 3** — 토큰 일관·#000 미사용·표면 3단계는 확보. 다만 상태를 색에만 싣는 구조가 두 곳(F-4 선택 3종, F-5 배지 아이콘 0개)에서 반복돼 5 로 못 간다.
- **UX 직관 3** — 빈/오류/로딩 상태 완성도는 높지만(위 C-6), T-2 마지막 단계 제출 버튼이 렌더에서 잘려 라벨이 안 보이고(F-1) T-3 시작점인 홈에서 비교 수치가 텍스트뿐(F-7)이라 핵심 과업 2개가 화면에서 막힌다.
- **적합성 5** — design.md §8 함정 2개(웨딩 클리셰·무채색 대시보드)를 둘 다 피했고, 다른 앱으로 옮기면 말이 안 되는 도메인 장치 3건이 실제로 렌더돼 있으며, 지인 회신 화면만 탭바 없이 큰 글씨로 분리해 §3 A-04 제약을 지켰다.

## 라우팅
`local` 7건은 그 속성만 고쳐 C 재검(B 로 안 감). `direction` 2건(F-6·F-7)은 2단계 "정보 표현" 축 재발산. `taste_gap` 0건. C_NOT_RUN 0화면(24/24 Read 로 열었다).
