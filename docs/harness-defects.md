# 하네스 실측 결함 로그

> 출처: 2026-09-05 `/design-harness docs/prd.md --budget 40m` 라이브 실행(세션명 test).
> 0-A 완료 시점에 사용자 kill-switch 로 중단. 아래는 **추측이 아니라 그 런에서 실제로 관측된 것**이다.
> 수정은 "하네스구현" 세션에서 한다.

## 관측 사실

| 항목 | 실측값 |
|---|---|
| 모드 | fast (`--budget 40m`) — 0단계 예산 12분 |
| 0-A 소요 | **6분 15초 / 81,309 토큰 / tool_uses 6** |
| 0-A 산출 | `design/prd_analysis.md` **278줄** |
| 그중 fast 모드에서 소비 가능한 양 | PRD 반박 15건 중 **2건**(★표시분), 미확정 12건 중 **0건**(fast 는 Q11b 생략) |

## D-1. fast 모드가 사람 개입만 압축하고 에이전트 산출량은 압축하지 않는다 (심각)

`design-harness/SKILL.md` 예산표는 인터뷰 12→6, 갤러리 타일 24→12, 월드컵, 토큰 세트 3→2, 열린 축 3→1 을 줄인다.
**전부 사람이 소비하는 양이다.** 0-A(PRD 분석)·0-C(갤러리 생성)·0-F(규칙화) 같은 에이전트 작업의 산출량 상한은 표에 한 행도 없다.

결과: 40분 예산에서 0-A 하나가 6분을 썼고, 그 산출의 대부분이 fast 모드에서는 **물어볼 수단이 없어 생성 즉시 사장**됐다.
`state.json.caps` 에도 에이전트 산출 상한 키가 없다(있는 것은 전부 사람 개입 상한).

### 수정 방향
- 예산표에 **에이전트 산출 상한 행** 추가. fast 기준 제안: 0-A 반박 ≤5 · 미확정 ≤4 · 화면흐름 1안 · 시나리오 2개, 0-F 기준 ≤12.
- `templates/state.json.caps` 에 대응 키 추가(`prd_pushback_max`, `prd_open_questions_max`, `scenarios_max`, `flow_candidates_max`, `rules_max`).
- 서브 브리프에 그 상한을 **문장으로** 넣도록 각 스킬에 명시. 상한을 표에만 적어 두면 서브에이전트는 못 본다.

## D-2. 서브에이전트 입력 파일 화이트리스트가 강제되지 않는다

`design-interview/SKILL.md` 0-A 는 "브리프에 넘길 것: PRD 경로, 출력 경로 `design/prd_analysis.md`" 라고만 적혀 있고,
**그 외 파일을 읽지 말라는 금지 문장이 없다.**

실제로 벌어진 일:
- 메인이 브리프에 `docs/concept.md` 와 `.claude/skills/design-interview/SKILL.md` 를 추가로 얹었다(정의 위반이지만 이를 막는 규칙이 없다).
- 서브에이전트는 거기에 더해 `design/interview_raw.md` 까지 읽고, **"인터뷰 기록이 비어 있으니 0-A 신규 작성이다"** 라고 판단했다.
  - 이 판단은 결론만 맞고 근거가 틀렸다. `interview_raw.md` 공백 검사는 **시작 절차 2번(전체 재시작 감지)** 의 규칙이며 메인 세션이 이미 판정한 것이다. 0-A 의 신규/재시작 판정 근거는 `design/prd_analysis.md` 존재 여부뿐이다.
  - 즉 **읽을 필요 없는 파일을 읽고, 남의 단계 규칙으로 자기 단계를 정당화**했다. 읽는 파일이 늘수록 토큰·시간이 늘고 이런 오작동이 늘어난다.

### 수정 방향
- "서브에이전트 위임 규칙" 에 **"입력 파일 화이트리스트 — 브리프에 나열된 경로 외의 파일을 읽지 않는다. 더 필요하면 `BLOCKED:` 로 되돌린다"** 를 명문화.
- 현재 문구 "브리프에는 파일 경로와 출력 경로만 넘긴다" 는 **메인에 대한 권고**일 뿐 서브에 대한 금지가 아니다. 둘을 분리해서 쓸 것.

## D-3. 종료조건이 하한(존재·개수 ≥N)뿐이라 과잉 생산이 무위험이다

0-A 종료조건: "8개 섹션이 전부 존재하고, 화면 표의 모든 행에 PRD 기능 번호가 매핑되어 있으며, 반박 목록 항목마다 '물을 쉬운 말' 이 있다."
0-B~0-H 종료조건도 전부 `≥` 형태다(§1 ≥3, §4 ≥8, §6 ≥1, §7 ≥3, §11 ≥2줄).

**상한이 하나도 없다.** 에이전트 입장에서 많이 쓰는 것은 항상 안전하므로, 상한 없는 게이트는 반드시 과잉 생산 방향으로 터진다. D-1 은 이 구조가 예산과 만나 발현된 것이다.

### 수정 방향
- 모든 종료조건의 `≥N` 옆에 `≤M` 병기. mode 에 따라 M 이 달라지므로 `state.caps` 를 참조하게 한다.
- `design-worker` 종료조건 검사가 상한 초과도 FAIL 로 잡도록 판정 항목에 추가.

## 파급 검토 (미확인 — 하네스구현 세션에서 확인할 것)

D-1/D-3 은 0단계 고유 문제가 아니다. 아래도 같은 구조일 가능성이 높으니 함께 볼 것:
- 0-C 갤러리: 타일 수 상한은 있으나 **타일 1장의 복잡도** 상한이 없다.
- 0-F 규칙화: "기준 개수 목표 8~20" 은 상한이 있는 유일한 곳이다. 이 문장을 다른 단계의 본보기로 쓸 수 있다.
- 2단계 `design-draft-html`, 3단계 `design-figma-build` 의 산출량 상한 — 미확인.

---

## 처리 결과 (하네스구현 세션, 2026-09-05)

| 결함 | 처리 | 위치 |
|---|---|---|
| D-1 | `state.caps` 를 `human_*` / `agent_*` 로 분리하고 `caps_fast` 추가. 예산표에 에이전트 산출 상한 표(0-A 줄 수·반박·미확정·흐름·시나리오·키워드, 0-C 타일 복잡도·파일 크기, 0-F 기준 수·brief 줄 수, design.md·초안 HTML·리포트 줄 수) 신설. 각 단계 스킬의 "브리프에 넘길 것" 에 **상한 문장**을 넣도록 명시 | `design-harness/SKILL.md` 예산표, `templates/state.json`, 각 단계 SKILL.md |
| D-2 | 위임 규칙을 **메인이 지킬 것 / 서브가 지킬 것**으로 분리. 브리프 필수 3요소(입력 화이트리스트·출력 경로·상한 숫자), 절차 문서 첨부 금지, 신규/재시작은 메인만 판정. 에이전트 정의 3종 맨 앞에 화이트리스트 강제·상한 준수 조항 추가 | `design-harness/SKILL.md`, `.claude/agents/*.md`, 각 단계 "브리프에 넘길 것" |
| D-3 | 0단계 종료조건 전항에 `≤M` 병기(§1 3~5, §4 rules_min~max, §6 1~15, §7 3~10, §10 ≤pushback_max, §11 2~6, brief 전체 줄 수). 0-A 종료조건에 상한 5종. 1·2·3단계 산출물에도 줄 수 상한. "상한 초과 = 하한 미달과 같은 FAIL" 을 오케스트레이터·에이전트 정의·종료조건에 명시 | 위와 동일 |
| 파급 | 0-C 타일 1장 요소 ≤6(fast 5)·파일 ≤200KB(fast 120), 2단계 초안 HTML ≤400줄(fast 300), 2·3단계 리포트 ≤80줄(fast 50) | `design-draft-html`, `design-figma-build` |

검증 지표: 새 세션에서 같은 명령 재실행 시 0-A 가 **6분 15초 / 278줄** 보다 유의미하게 작아야 한다. fast 상한은 60줄이다.

---

# 2회차 런 (2026-09-05, 세션 test2) — 0단계 완주에서 나온 결함

> 1회차 수정(D-1·D-2·D-3) 검증 겸 0-A~0-H 를 끝까지 돌린 런. 아래는 그 과정에서 **실제로 관측된 것**이다.

## 검증 결과 요약 (D-1·D-2·D-3)

| 항목 | 1회차 | 2회차 | 판정 |
|---|---|---|---|
| 0-A 소요 / 줄 수 | 6분 15초 / 278줄 | **1분 54초 / 48줄** | D-1 해소 |
| 0-A 반박 생성 / 소비 | 15건 / 2건 | **4건 / 2건** | 사장분 0 |
| 서브 tool_uses (0-A) | 6 | **2** | — |
| 화이트리스트 준수 | 미측정 | **4/4 위임 전건 준수** | D-2 해소(아래 단서) |
| 상한 초과 FAIL | 미측정 | **0건** (brief 143줄 초과를 메인이 138줄로 압축) | D-3 작동 |

D-2 측정 방법: 각 브리프에 "완료 보고 맨 앞 줄에 `READ:` 로 실제 읽은 파일을 나열하라"를 넣어 자기보고를 받았다. 0-C(입력 2개)·0-F(입력 6개)·QA 채점(입력 1개, 금지 파일이 바로 옆에 있음)·0-G(입력 1개) 모두 화이트리스트와 정확히 일치했다. 특히 QA 채점은 `gallery_index.json`(축 정답표)을, 0-F 는 `qa_scores.json`(점수표)을 읽으면 편한 상황이었는데 열지 않았다.
**단서**: `READ:` 는 서브의 자기보고이므로 완전한 증거가 아니다. 확실히 하려면 메인이 파일 접근 로그를 따로 봐야 한다. `BLOCKED:` 반환은 이번 런에서 한 번도 발생하지 않아 그 경로는 여전히 미검증이다.

## D-4. 0-G 감사 입력에 질문이 빠져 답변형 근거가 무근거로 판정된다 (심각)

`design-interview/SKILL.md` 0-G 는 감사 입력을 `{rule_id, source_quote, statement}` **3필드**로 규정한다.
그런데 인터뷰 답변은 질문에 의존하는 짧은 발화다 — `"있음 — 어른도 본다"`, `"링크만 열면 끝"`, `"아니오, 한 눈에"`.
질문을 가리면 감사자는 뿌리를 볼 수 없다.

실측: 3필드로 1차 감사 → **entailed 0 / over_generalized 5 / unsupported 1**. RULE-06(하드 제약 `[CONSTRAINT]`)이 "두 인용 모두 글자 크기·대비를 언급하지 않아 뿌리 자체가 없다"로 `unsupported` 판정을 받았다. 그 언급은 **질문 쪽**에 있었다("큰 글씨나 진한 대비가 꼭 필요한 사용자가 있나요?").
질문+답변 쌍으로 복원해 재감사 → **entailed 1 / over_generalized 4 / unsupported 1**. RULE-06 은 "방향은 합의된 내용, 수치만 넓다"로 바뀌었고 RULE-02 는 entailed 로 올라갔다.

전건 강등은 0단계 산출물을 통째로 참고용으로 만든다. 감사가 무의미해지는 것이 아니라 **반대로 작동한다** — 통과가 불가능한 감사는 신호를 주지 못한다.

### 수정 방향
- 감사 입력을 `{rule_id, statement, evidence[]}` 로 바꾼다. `evidence` 항목마다 **종류**를 표시한다: `질문과 답변` / `대비쌍에서 한쪽을 고른 결과`(무엇과 무엇 중에서 골랐는지 포함) / `타일 하나에 대한 반응`.
- 감사 브리프에 **종류별 증거력**을 명시한다: 질문에 담긴 내용도 합의된 것으로 본다 · 대비쌍 선택은 이유가 없어도 방향 증거다 · 이유 없는 "애매"는 선호 정보가 없는 것이다.
- 가려야 할 것은 **하네스의 해석**(축 이름·근거 ID·confidence)이지 **자극의 객관적 속성**이 아니다(D-5 참조).
- 판정만 받지 말고 `narrowed`(근거가 지지하는 만큼으로 좁힌 주장)를 함께 받는다. 이번 런에서 실질 산출은 전부 여기서 나왔다 — "1.5배"·"16px"·"4.5:1"·"배지로만" 같이 하네스가 채워 넣은 값이 이 필드로 걸러졌다.

## D-5. 축을 가리면 타일 반응 기반 기준은 전부 무근거가 된다

D-4 와 짝이다. 0-G 는 "축·근거 ID·문맥을 가린다"고 규정한다. 그런데 타일 반응의 이유 원문은 대개 축을 지목하지 않는다 — `"깔끔함"`, `"밋밋한 느낌"`.
축을 가린 감사자는 그 반응이 **어느 타일의 무엇에 대한 것인지** 알 수 없어 방향 자체를 무근거로 판정한다.

실측: RULE-03(형태 축)이 `unsupported` — "이유 문구에 모서리·카드·구분선을 가리키는 말이 전혀 없어 그 타일이 어느 축의 어느 쪽이었는지 알 수 없다". 사용자는 실제로 G-02(둥근 카드)에 "좋다 — 깔끔함", G-09(각진 구분선)에 "애매"를 줬고 `gallery_index.json` 에 그 매핑이 있다. **감사자만 몰랐다.**

### 수정 방향
- 근거에 **자극의 객관적 속성**을 붙인다(예: "이 타일은 모서리가 둥근 카드형, 비교 대상은 각진 구분선형"). 이것은 하네스의 해석이 아니라 자극의 사실이다.
- 계속 가려야 하는 것: 축 이름·`confidence`·`source_refs` ID·하네스가 그 반응을 어떻게 해석했는지.
- 이 구분을 SKILL.md 0-G 에 **"가리는 것 / 주는 것" 두 목록**으로 명시한다. 현재 문구("축·근거 ID·문맥은 가린다")의 '문맥'이 너무 넓다.

## D-6. 0-D 반응 수집이 채팅 타이핑 전제라 비전문가 부담이 크다 (개선 제안, 사용자 피드백 발)

디자이너 피드백에서 나온 것: 갤러리를 `open` 으로 열고 사용자가 채팅에 `G-03 좋다 - ...` 를 12줄 치는 구조는 부담이 크다.
이번 런에서 갤러리·월드컵을 **Claude Artifact + `db` capability** 로 발행해 클릭·한 줄 입력으로 받고, 메인이 `read_db` 로 회수했다. 실측: 12타일 전건 + 월드컵 2건 회수 성공, 사용자 타이핑은 이유 3줄뿐.

부수 설계 결정 두 가지가 실제로 값을 했다:
- **QA 점수는 사람이 매기지 않고 하네스가 매긴다.** 디자이너 제안은 사용자가 5축(컬러 일관성·타이포·간격·정렬·접근성)을 점수화하는 것이었으나, 비전문가에게 라벨형 질문이 되어 하네스 1번 원칙에 걸린다. `design-judge` 가 채점하고 사용자는 좋다/싫다/애매만 남기게 했다.
- **점수는 판정 후 공개한다.** 먼저 보여 주면 반응이 점수의 메아리가 된다(1단계 토큰 선택의 "선택 기록 후 AI 추천 공개"와 같은 패턴). 판정을 바꾸면 `changedAfterReveal` 로 기록했고, 실측 4건이 찍혔으나 최종 판정이 실제로 바뀐 것은 1건(G-07)뿐이었다 — 앵커링 방지가 작동했다.
- **접근성 대비는 사람에게 묻지 않는다.** 페이지가 `getComputedStyle` 로 계산해 배지로 표시한다. 계산 가능한 것에 사람의 시간을 쓰지 않는다.

### 구현 주의 (실측 함정)
`db` 의 `collection().get()` 은 배열이 아니라 `QuerySnapshot`(`.docs`)을 반환하고, 문서 본문은 `data` 속성이 아니라 **`data()` 함수**다. 이를 틀리면 첫 읽기에서 `TypeError` 가 나는데, 그 예외를 바깥 `catch` 가 잡아 "연결 실패"로 표시하면 **저장은 멀쩡한데 안 된다고 잘못 안내**하게 된다. 읽기 실패와 연결 실패를 분리하고, 실패 시 에러 코드를 화면에 노출할 것.

## D-7. 1-B 대비 보정이 primitive 를 직접 바꿔 스케일을 깨뜨린다

`border.default` 가 `{neutral.200}` 을 참조하는데, 대비 미달을 고치라고 하면 서브가 **참조 대상인 primitive 를 어둡게** 만든다. 그러면 그 primitive 를 함께 쓰는 다른 토큰이 전부 끌려간다.

실측: neutral.200 이 300 보다 어두워져 스케일이 역전됐고 `border.strong` 이 `border.default` 보다 연해졌다. **대비 검사는 통과하므로 기계 검사로 안 잡힌다.**

### 수정 방향
- 보정 브리프에 "primitive 를 바꾸지 말고 semantic 참조를 다른 단계로 옮기거나 단계를 신설하라" 명시.
- 1-B 종료조건에 **스케일 단조성**과 **strong > default** 검사 추가.

## D-8. 병렬 위임이 공유 JSON 을 동시에 써서 파일이 깨진다

화면별 조립을 병렬로 내면 각 에이전트가 `figma_nodes.json` 의 자기 섹션을 쓰는데, 읽기-수정-쓰기가 겹쳐 **중복 키가 생기고 JSON 이 무효**가 됐다. 뒤에 끝난 에이전트가 우연히 발견해 병합했을 뿐이다.

### 수정 방향
산출을 에이전트마다 **별도 파일**(`figma_nodes.<screen>.json`)로 받고 메인이 합치거나, 노드 ID 를 반환값으로만 받아 메인이 단독으로 쓴다.

## D-9. 토큰 CSS 변환에서 한글 키가 뭉개진다

`tokens.json` 의 상태 색 키가 한글이다(`status.대기`·`확정임박`·`확정`·`완료`). 변환기가 `[^a-z0-9]` 를 제거하면 **9종이 3종으로 충돌**해 상태 구분이 통째로 사라진다. 화면은 정상으로 보이므로 눈으로도 안 잡힌다.

### 수정 방향
한글 키에 영문 슬러그 매핑을 두고, 변환 후 **입력 키 수 == 출력 변수 수** 를 검사한다.

## D-10. "확인했습니다" 를 믿으면 세 번까지 틀린다 (심각)

이번 런에서 가장 비싼 결함이다. 탭바 아이콘이 화면에서 네모로 보인다는 지적을 **사용자가 세 번** 했다.

| 회차 | 무슨 일 | 왜 못 잡았나 |
|---|---|---|
| 1 | 에이전트가 아이콘 제작 후 "정상 렌더링 확인" | 메인이 **마스터 컴포넌트만** 스크린샷으로 봤다 |
| 2 | 복구 에이전트가 "스크린샷 육안 확인" 보고 | 메인이 그 보고를 그대로 전달했다 |
| 3 | 메인이 화면을 직접 찍음 → 여전히 네모 | 여기서야 원인 추적 시작 |

원인은 아이콘 내부 `duo-fg` 프레임의 **불투명 fill 이 같은 색 벡터를 덮은 것**이었다. 마스터에서는 렌더가 달라 보였고 인스턴스에서만 드러났다.

스킬에 "서브의 완료 보고는 검증 근거가 아니다" 가 이미 적혀 있는데 지켜지지 않았다. 문장이 있는 것과 작동하는 것은 다르다.

### 수정 방향
- **시각 결과물은 메인이 최종 산출물(화면 프레임)을 직접 열어 확인**한다. 컴포넌트·마스터 확인은 대체가 되지 않는다.
- 3-D A검사 항목에 "아이콘 벡터가 살아 있는가"처럼 **덮임을 잡는 기계 검사**를 넣는다(벡터 개수·덮개 fill 유무).
- 같은 지적이 **2회 반복되면 위임을 멈추고 메인이 직접 진단**한다. 이번엔 3회째에야 그렇게 했다.

## D-11. 프레임을 hug 로 두면 앱 화면으로 보이지 않는다

2단계가 화면 크기 규격을 정하지 않아 프레임 높이가 내용에 따라 400~900px 로 제각각이었다. 상태바·탭바도 없었다. **사용자가 다른 팀 시안과 비교해서야** 드러났다.

### 수정 방향
`design-draft-html` 에 **기기 프레임 규격**(모바일 390×844, 상태바·탭바 포함 여부)을 명시하고, 2단계 종료조건에 "모든 화면이 같은 규격인가" 를 넣는다.

## D-12. 시간 압박에서 화면을 고르면 PRD 기능이 빠진다

2단계에서 시간을 아끼려고 핵심 3화면만 골랐는데, 그 결과 **PRD F1(지인 등록)·F2(모임 편성) 화면이 통째로 없었다.** C 판정의 UX 항목이 "핵심 과업 2개 완주 불가" 로 잡아냈지만, 그건 3단계 끝이라 되돌리는 비용이 컸다.

### 수정 방향
2단계 종료조건에 **"PRD 기능 번호 전부가 최소 한 화면에 매핑되는가"** 를 넣는다. 0-A 화면 흐름표가 이미 그 매핑을 갖고 있으므로 대조만 하면 된다.

## 처리 결과 2 (하네스구현 세션, D-4~D-12)

| 결함 | 처리 | 위치 |
|---|---|---|
| D-4 | 감사 입력을 `{rule_id, statement, evidence[]}` 로, evidence 종류(qa/pair/tile)별 필수 사실·증거력 명시, `narrowed` 판정 필드 추가(over_generalized 는 statement 교체·confidence 유지) | `design-interview` 0-G, `references/rule_schema.md`, `templates/brief.md` |
| D-5 | "가리는 것 / 주는 것" 두 목록으로 분리. 자극의 객관적 속성·질문 원문은 준다 | 위와 동일 |
| D-6 | 갤러리·월드컵 페이지에 반응 UI 내장, Artifact `db` 로 저장, 메인이 `read_db` 회수. QA 점수는 judge 가, 판정 저장 후 공개, 접근성은 페이지 계산. `QuerySnapshot`/`data()` 함정과 읽기·연결 실패 분리 명시. 폴백(open+채팅) 유지 | `design-interview` 0-C·0-D |
| D-7 | 보정 브리프에 "primitive 불변, semantic 참조 이동/단계 신설" 문장. 종료조건에 스케일 단조성·강약 관계 검사 | `design-tokens` 1-B |
| D-8 | 병렬 산출은 에이전트별 별도 파일, 병합은 worker 단일 호출. 오케스트레이터 공통 규칙으로도 추가 | `design-harness`, `design-figma-build` 3-B·3-C |
| D-9 | tokens.json 상태 키 영문 슬러그 + `label` 규칙. 변환기는 한글 키 BLOCKED, 입력 leaf 수 == 출력 변수 수 검사 | `templates/tokens.json`, `design-draft-html` 2-A |
| D-10 | 3-G 첫 항목 = 메인이 화면 프레임 스크린샷을 직접 열어 확인(위임 불가). A검사 7 아이콘 덮임(인스턴스 대상). 라우팅표에 "사용자 지적 2회 = 위임 중단·메인 직접 진단". 오케스트레이터에 "완료 보고 수치 인용 금지" | `design-figma-build` 3-B·3-D·3-F·3-G, `design-harness` |
| ③ 겹침·크기 | 컴포넌트 배치 y+height+여백, A검사 8 크기 sanity ±30%, spacing 은 키가 아니라 값 | `design-figma-build` 3-B·3-D |
| D-11 | 기기 프레임 규격 390×844(상태바·탭바) 고정, hug 금지. 2단계·3단계 종료조건에 규격 동일성 | `design-draft-html` 2-C·종료조건, `design-figma-build` 3-C·A검사 9 |
| D-12 | 2-F "화면을 빼지 않는다, 빼야 하면 상태 변형을 줄인다". 종료조건에 PRD 기능 전부 매핑 검사 | `design-draft-html` 2-F·종료조건 |
| 확산 | "가장 중요한 것부터, 초과분은 쓰지 않는다" 를 모든 브리프 공통 문장으로 | `design-harness` 메인이 지킬 것 |

미검증 잔여: `BLOCKED:` 반환 경로(아직 0건), 화이트리스트는 `READ:` 자기보고 기반.

---

# 3회차 표적 검증 (2026-09-06, 세션 test2)

전체 런 대신 **결함을 직접 만들어 놓고 검사기가 잡는지** 확인했다. 전체 런은 결함이 재현되기를 기다려야 하지만, 표적 시험은 검출력 자체를 본다 — 결함이 없는 상태에서 PASS 가 나온 것과 검사기가 작동하는 것은 다른 사실이다.

## 결과 요약

| 시험 | 대상 | 결과 |
|---|---|---|
| 1 | 2-A `BLOCKED` 경로 | **통과** — 3개 런 만에 처음 밟혔다 |
| 2 | A검사 7 아이콘 덮임 | **통과** — 합성 케이스 good=PASS / bad=FAIL |
| 3 | 2단계 종료조건 PRD 매핑 | **통과** — 화면 2개 제외 가정에서 F1 누락을 FAIL 로 검출 |

## 시험 1 — BLOCKED 경로 (D-2 잔여 미검증분 해소)

`tokens.json` 이 이미 한글 키 9개(`status.대기`·`response.가능`·`alert.겹침경고` 등)를 갖고 있어 별도 주입 없이 시험이 됐다.

서브가 `BLOCKED: 한글 키 <경로 9개>` 로 되돌리며 **"ASCII 키로 슬러그화하는 것은 판단이므로 메인에 의뢰 필요"** 라고 명시했다. 자기 판단으로 슬러그를 만들어 진행하지 않은 것이 핵심이다 — 2회차 런에서는 메인이 임의 슬러그 매핑을 만들어 넣었고 그 매핑이 어디에도 기록되지 않았다.

1·2회차 런에서 `BLOCKED` 는 0건이었다. 경로가 살아 있음이 처음 확인됐다.

## 시험 2 — A검사 7 검출력

`__TEST__` 페이지에 합성 케이스 2개를 만들어 시험하고 삭제했다.

- `good`: 벡터만, 컨테이너 fill 없음 → PASS
- `bad`: 벡터와 **같은 색** 불투명 사각형이 벡터 뒤 인덱스에 24×24 → **FAIL 정확히 검출**

실제 Screens 24프레임은 덮개 0건(아이콘 인스턴스 9, 보이는 벡터 90). D-10 케이스는 이미 고쳐져 재현되지 않았으므로 합성으로 검출력을 따로 확인했다.

## 시험 3 — 종료조건이 실제로 FAIL 을 내는가

`contacts_list`·`contact_edit` 를 목록에서 제외했다고 가정하니 **F1(지인 등록·관계별 묶기)이 어느 화면에도 매핑되지 않아 FAIL** 이 났다. D-12 에서 이 누락이 3단계 끝에서야 드러났던 구멍이 닫혔다.

## D-13. 서브가 "파일 참조" 라고 보고하고 파일을 만들지 않는다

시험 2 를 맡은 서브가 판정 내용을 메시지로 보고하며 `세부: design/verify/a7_icon_cover_test.md` 라고 적었으나 **그 파일이 존재하지 않았다.** 판정 내용 자체는 메인이 재현해 확인한 결과 맞았다(덮개 0건, 벡터 90개).

D-10 과 같은 부류이되 형태가 다르다. D-10 은 "확인했다"가 거짓이었고, 이것은 "파일에 썼다"가 거짓이다. 결과가 맞아도 **근거 파일이 없으면 나중에 아무도 검증할 수 없다.**

### 수정 방향
- 리포트 산출을 요구한 위임은 **메인이 파일 존재와 줄 수를 확인**한 뒤에만 완료로 친다. 보고 메시지에 적힌 경로를 그대로 인용하지 말 것.
- 종료조건 검사에 "리포트 파일이 실제로 존재하고 비어 있지 않은가" 를 넣는다.

## D-14. 2단계 HTML 8개 중 4개가 프레임 규격 없이 남았다 (실제 결함)

시험 3 (3) 에서 **시험이 아니라 진짜 문제**가 나왔다.

| 규격 있음 (390×844 + 상태바·탭바) | 규격 없음 (responsive) |
|---|---|
| contacts_list · contact_edit · group_compose · date_propose | **onboarding · home · meeting_detail · guest_reply** |

뒤의 4개는 사용자가 규격 문제를 지적하기 **전에** 만들어졌고, 앞의 4개는 지적 **후에** 만들어졌다. Figma 프레임은 메인이 나중에 일괄로 844 고정했기 때문에 **Figma 는 맞고 HTML 만 어긋난 상태**다.

새 스킬의 종료조건이 아니었으면 이 불일치는 발견되지 않았다. 산출물(Figma)에는 영향이 없으나, HTML 초안이 "승인의 근거" 라는 하네스 전제가 깨진다 — 승인받은 HTML 과 구현된 Figma 의 규격이 다르다.

### 수정 방향
2-F 에서 화면을 나눠 병렬 생성할 때 **규격을 브리프의 고정 문장으로** 넣고(이미 반영됨), 종료조건의 규격 동일성 검사를 **2-H 승인 전에** 돌린다. 지금은 검사가 종료조건에만 있어 승인 후에 걸린다.

## 처리 결과 3 (하네스구현 세션, D-13·D-14)

| 결함 | 처리 | 위치 |
|---|---|---|
| D-13 | "보고 메시지의 파일 경로 인용 금지 — 메인이 존재·줄 수 확인 후 완료" 를 서브 규칙에 추가. 모든 단계 종료조건 공통 항목 "보고한 산출 파일이 실제 존재·비어 있지 않음" 추가 | `design-harness` 서브 규칙·단계 전환 규칙 |
| D-14 | 2-G 에 승인 전 구조 검사 2종(프레임 규격 동일·PRD 기능 전부 매핑)을 넣고, 2-H 첫 항목을 "그 검사 PASS 확인, FAIL 이면 승인 화면을 열지 않음" 으로. 승인 후 규격·화면 수 변경 시 승인 무효·재승인 | `design-draft-html` 2-G·2-H |

미검증 잔여: 3-G 최종 스크린샷 기록, 0-G 감사 입력 형식 — 전체 런 필요.

## D-15. 완료된 서브가 나중에 다시 실행되어 옛 결과를 덮어쓴다

시험 2(A검사 7)에서 실제로 벌어졌다.

1. 서브가 1차 완료 보고: `good=PASS, bad=FAIL, DETECTOR 작동함`. 이때 시험용 `__TEST__` 페이지가 존재했다.
2. 메인이 페이지를 **삭제**하고 리포트를 작성했다.
3. **같은 서브가 나중에 다시 실행**되어 2차 보고를 냈다: `bad=PASS(벡터 0개) → 검사기 결함`. 그리고 메인이 쓴 리포트 파일을 **덮어썼다**.
4. 2차는 페이지가 사라진 뒤의 검사라 무효였다. 메인이 케이스를 재생성해 구조를 실측하고 직접 검사·육안 확인해 1차가 맞았음을 확정했다.

두 가지가 겹쳤다.

**(a) 시험 환경을 검사 완료 전에 철거했다.** 메인이 서브의 1차 보고만 보고 페이지를 지웠다. 서브가 재실행될 수 있다는 것을 고려하지 않았다.

**(b) 합성 케이스의 구조를 만든 직후 확인하지 않았다.** 이것이 더 중요하다. 2차 보고가 없었다면 메인은 케이스에 벡터가 실제로 들어갔는지 **한 번도 확인하지 않은 채** "검출기 작동함"이라는 결론을 피어에게 보냈을 것이다. 케이스가 잘못 만들어졌으면 "작동함"도 "결함"도 둘 다 근거가 없다.

D-10 과 같은 뿌리다 — **만든 것을 만든 직후 확인하지 않는 습관.** 이번엔 대상이 산출물이 아니라 시험 도구였다.

### 수정 방향
- **시험용 자산은 리포트가 파일로 확정되고 메인이 그것을 읽은 뒤에 철거한다.**
- **합성 케이스는 생성 스크립트의 반환값에 구조 실측치를 포함한다**(벡터 수·visible·크기·덮개 유무·opacity). 반환값에 없으면 만든 것이 아니다.
- 서브 완료 보고가 **같은 task 로 두 번 이상 오면** 나중 것이 최신이라고 가정하지 말고, 두 보고의 전제(파일·노드 존재 여부)가 같은지 먼저 확인한다.

## 처리 결과 4 (하네스구현 세션, D-15)

| 결함 | 처리 | 위치 |
|---|---|---|
| D-15 | 서브 규칙 3건 추가: ①생성 브리프는 반환값에 구조 실측치 요구(노드 ID 만은 미완료), 검증 도구에도 동일 적용 ②시험 자산은 리포트 파일 확정·메인 열람 후 철거 ③같은 task 완료 보고가 2회 이상이면 전제 대조 후 판단, 리포트는 덮어쓰기 대신 번호 붙여 보존. design-maker 정의 6항과 3-B 반환 규칙에 실측치 필수 | `design-harness` 서브 규칙, `.claude/agents/design-maker.md`, `design-figma-build` 3-B |

# 4회차 검증 1 (2026-09-07, 세션 test2) — 결정론 검사기 실제 Figma 실행

## D-16. extract-nodes.js 가 선택 없는 상태에서 PAGE 노드를 serialize 해 크래시
`[figma.currentPage]` 를 루트로 넘기는데 `serialize` 첫 줄이 `node.visible` 을 읽어 `TypeError: no such property 'visible' on PAGE node`. "Screens 페이지 선택 상태" 전제는 사람 손을 요구한다.
**처치**: 루트를 `figma.currentPage.children` 으로, `visible` 은 `'visible' in node` 가드. 판정 핵심을 `scripts/lib/audit-core.js` 로 빼면서 `serializeNode` 도 같은 가드로 통일.

## D-17. 노드 덤프를 디스크로 내릴 다리가 없다 (심각 · 설계 결함)
`use_figma` 는 로컬 파일에 쓸 수 없고 반환값은 약 20KB 에서 잘린다. 24프레임 덤프가 553KB(1289 노드) — 청크로 받으면 컨텍스트 약 15만 토큰. 문서의 "출력을 nodes.json 으로 저장한 뒤" 는 누가 어떻게 하는지 정의되지 않았다.
**처치(2번 안 채택 — 판정을 Figma 안에서 완결)**: `scripts/make-figma-audit.js` 가 컴파일된 규칙 + `audit-core.js` 소스를 하나의 `use_figma` 코드(약 15KB)로 묶는다. Figma 안에서 순회·판정하고 **위반 목록만 반환**(규칙당 기록 상한 25, 개수는 전부, 실측 반환 2KB). `audit.js --render` 로 텍스트 리포트. 결정론 게이트라면 판정까지 결정론이어야 한다는 원칙과도 맞는다. `extract-nodes.js` 는 작은 프레임을 사람이 들여다볼 때만.

## 부수 관찰 처치
- build-rules.js 가 한글 키를 통과시킴 → **비ASCII 키 거부**로 변경(2-A 변환기와 같은 정책, D-9). 색 자체는 hex 목록이라 감사 대상에서 빠진 것은 아니었으나 정책 불일치가 문제.
- variant required 0 → 파서가 마크다운 표의 "상태/variant" 열을 읽도록 강화, `--variants "a,b,c"` 명시 옵션 추가, 0건이면 경고 출력.
- `min_font_size`·`min_size` 검사기 구현 추가(미구현 10 → 7). 남은 미구현(contrast·image_fill·text_overflow·saturation·style_bound·reuse_ratio·variant_states)은 여전히 사람 게이트 승격.

# 4회차 검증 1 재실행 (2026-09-07, test2) — 번들 방식 4단계 통과. 판정 오탐·구조 결함 3건

## D-18. 이름 기반 인터랙티브 추정으로 터치 타깃 오탐 128건
`Chip/Status`(상태 배지)·`Icon/*`(탭 안의 그림)이 이름만으로 인터랙티브로 잡혀 44×44 미달로 판정. 상태 배지는 누르는 것이 아니고, 탭바의 터치 타깃은 아이콘이 아니라 감싼 `Tab/*` 프레임이다.
**처치**: 이름 목록에서 Chip·Icon·Row 제거(reactions 우선). **인터랙티브 조상이 있으면 자식은 제외**(터치 타깃은 가장 바깥 것 하나). 인스턴스 내부 노드 제외 옵션(`exclude_instance_children`)을 core 규칙에 적용. 재현 테스트: Tab 안 Icon·Chip 미검출, 343×30 버튼만 검출.

## D-19. 인스턴스 내부 자동 생성 `Vector` 90건이 네이밍 위반으로
`createNodeFromSvg` 가 만드는 자식 벡터 이름은 `Vector` 이고 부모(`Icon/Home`)가 이미 의미 이름이다. 아이콘 path 하나하나에 역할 이름을 요구하는 것은 과하다.
**처치**: 인스턴스 내부(id 에 `;`)는 네이밍 검사 제외, 의미 있는 부모 아래의 자동 생성 이름(Vector·Union·Ellipse…)은 통과. 최상위에 남은 `Vector` 는 여전히 잡는다.

## D-20. 미구현 blocker 가 `passed` 를 영구 false 로 만든다 (구조)
`passed = blocker==0 && unchecked==0` 이라 검사기가 반쯤 구현된 동안 어떤 프로젝트도 A게이트를 통과할 수 없었다. 게이트가 정보만 주고 문을 여닫지 못했다.
**처치**: `passed_machine`(구현된 검사 기준)과 `requires_human_review`(미구현 blocker 목록)를 분리. 종료 코드·게이트 판정은 `passed_machine`, 미구현분은 3-G 사람 게이트로. `style_bound`(textStyleId 바인딩)·`variant_states_present`(COMPONENT_SET 의 variantProperties, **컴포넌트별 요구 목록**) 구현 → 미구현 7→5. 남은 것: contrast 2종(색 쌍 해석 필요)·image_fill·text_overflow·reuse_ratio.

## 부수
- variant 파서: 괄호 안 쉼표를 구분자로 보지 않도록, `Key=Value` 는 Value 만. 컴포넌트별 요구 목록(`required_by_component`)을 표 1열(이름)에서 만든다 — 전역 목록을 모든 세트에 요구하던 문제(Avatar 에 hover 요구) 해소.
- 진짜 위반 17건(RowLabel 하드코딩 색 16 + paddingBottom 88)은 검사가 아니었으면 못 찾았을 것 — 검사기의 존재 이유가 실측됨.
- 마이그레이션: 한글 키 tokens.json 은 1단계 확정 시점부터 슬러그여야 한다. 이미 Figma Variables·tokens.css 와 물린 프로젝트는 셋을 함께 바꾸는 절차가 필요(이번엔 사본 `tokens.slug.json` 으로 우회). design-tokens 종료조건에 명시.
- use_figma 코드 상한 50,000자 실측. 번들 16~18KB 로 여유.

# 4회차 검증 1 3차 (2026-09-07, test2) — 오탐 해소 확인, 잔여 3건 처치

- naming 90→0, touch-target 128→25(실제 컨트롤만), 진짜 위반 33건 유지(RowLabel 색 16 + 스타일 16 + spacing 1 — 전부 제작자 실수, 눈으로 안 보임). `type-style-reuse` 신규 검출 16건은 mixed 0·인스턴스 내부 0 으로 정확.
- **D-21. variant 검사 미발동 — Components 페이지를 안 봄.** `--page Screens` 만 돌려 COMPONENT_SET 0개 → 0건이 통과처럼 보였다. 처치: 리포트에 `applicable_per_rule`·`not_applicable`(적용 대상 0개) 추가, `audit.js --render a.json,b.json` 병합, 절차를 "페이지마다 번들 1회 → 병합"으로. N/A 는 통과가 아니라고 명시.
- **D-22. required_by_component 미생성.** 파서가 앞 표의 "상태 메모" 열에 걸렸다. 처치: 헤더는 다음 줄이 구분선인 표 줄만, variant 열 우선·'상태'는 정확 일치만. 생성 로그에 사용한 표·열·컴포넌트 수 출력.
- **D-23. 터치 타깃 정책.** 이름 추정으로 blocker 를 잠그면 Row 포함/제외 어느 쪽이든 오탐(배지 vs 행 안 체크박스). 처치: `touch-target-min`(blocker) 은 reactions 있는 노드만, `touch-target-min-inferred`(warning) 은 이름 추정 — 사람 게이트가 본다. reactions 있는 노드는 추정 규칙에서 제외(중복 방지).

# 4회차 검증 1 4차 (2026-09-07, test2) — **A게이트 첫 통과** (Screens·Components 둘 다 passed_machine true)

- D-21·D-23 확인. 진짜 위반 33건을 고치자 blocker 정확히 0 — 오탐이 남아 있었다면 여기서 드러났을 것.
- **D-22 재발**: components.md 가 표가 아니라 산문(`## 1. Chip/Status (id 4:12)` + `variant: \`Status=Waiting\`(대기) · …`)이었다. 형식이 스킬에 고정돼 있지 않아 maker 가 임의로 썼고, test2 가 파일을 열지 않고 형식을 전언한 것이 한 라운드를 낭비시켰다(보고 전 실물 확인 규칙의 세 번째 위반 사례 — D-10·D-15와 같은 뿌리). 처치: ①2-F 에 components.md **표 형식 고정**(4열, 크기 열 포함) ②build-rules 에 산문 형식 폴백(섹션 제목 이름 + `variant:` 줄, `·` 구분, 백틱 Key=Value 의 Value).
- **D-24 COMPONENT_SET radius 오탐 7건**: variant set 컨테이너의 cornerRadius 5 는 Figma 기본값. 처치: radius 규칙에서 COMPONENT_SET 제외(`exclude_node_types`).
- 남은 미구현(사람 게이트): contrast-text-aa · image-fill-valid · text-not-clipped. 소요: 4단계 약 3분.

# 검증 1 최종 (2026-09-07) — 종료

| 라운드 | 결과 |
|---|---|
| 1차 | extract-nodes PAGE 크래시(D-16), 553KB 덤프로 파이프라인 실행 불가(D-17) → 판정을 Figma 안에서 완결하는 번들 방식 채택 |
| 2차 | 번들 완주. 오탐 218/235(터치 타깃 128·네이밍 90), passed 영구 false(D-18~D-20) |
| 3차 | 네이밍 0, 터치 타깃 실제 컨트롤만. variant 미발동·컴포넌트별 목록 미생성(D-21~D-23) |
| 4차 | **A게이트 첫 통과**(Screens·Components passed_machine true). 진짜 위반 33 → 수정 후 0. COMPONENT_SET radius 오탐(D-24), 산문형 components.md(D-22 재발) |
| 5차 | required_by_component 자동 생성 6/6 일치. 종료 |

검사기 신뢰도 근거: 잡은 33건이 전부 진짜였고 고치자 정확히 0. 오탐이 남았다면 33→0 이 나오지 않는다.

## D-25. "실물을 열어 확인" 규칙이 문장으로는 지켜지지 않는다 (구조)
같은 뿌리의 위반 3회(D-10·D-15·D-22). 처치: 오케스트레이터와 에이전트 정의 3종에 **보고의 `EVIDENCE:` 블록 강제** — 읽은 파일은 경로:줄, 본 노드는 id+실측치, 스크린샷은 파일명+위치+본 것, 명령은 종료 코드. 근거 블록 없는 보고는 미완료. 근거를 적으려면 열 수밖에 없다.

## 다음 런에 넘기는 잔여 위험
1. components.md 표 형식 고정(2-F)이 실효를 내는지 — maker 가 실제로 표로 쓰는지. 산문 폴백에 계속 기대면 형식이 흔들린다.
2. **tokens.json 슬러그 마이그레이션 경로 없음** — 기존 프로젝트(한글 키)를 새 스킬로 재감사하려면 tokens.json·tokens.css·Figma Variables 이름을 함께 바꾸는 절차가 필요. 새 프로젝트는 1단계부터 슬러그라 해당 없음. (미구현, 필요해지면 `scripts/migrate-token-keys.js` 로)
3. 미구현 3종(contrast·image_fill·text_overflow)이 `requires_human_review` 로 나간다. contrast 는 조상 체인에서 실제 배경 찾기가 필요해 비용 큼. 3-G 사람 게이트가 이것을 실제로 확인하는지가 다음 런의 관전 포인트.

## D-26. 주 행동이 프레임 아래로 잘린 채 승인됨 (오너 지적)
회신 화면의 주 버튼 "선택 완료" 가 844 아래로 잘려 캔버스에서 보이지 않았다. 프레임 규격 고정(D-11)만으로는 "무엇이 잘렸는가" 를 못 본다. 현업 관행은 기기 프레임 고정 + 스크롤 프로토타입 + 리뷰용 긴 프레임이며, 주 행동은 잘리게 두지 않고 하단 고정 바로 만든다.
**처치**: ①2-C/2-F 규칙 — `data-role="primary-action"` 은 `data-fixed="bottom"` 바 안이거나 above-fold 선언, 넘치는 화면은 `data-state="full"` 섹션. check-html H-9·H-10. ②3-C — `Action/Primary` 는 첫 화면 안 또는 `Bar/Action`, 넘치는 화면은 `/ full` 프레임(hug 허용) + `Guide/Fold`. A검사 13·14. ③c_checks C-6 에 "주 행동이 스크롤 없이 보이는가". ④design.md §2 에 주 행동 위치 한 줄.

## D-27. 인터뷰가 채팅 타이핑 전제 (오너 제안 → 개선)
갤러리·월드컵은 D-6 에서 아티팩트로 옮겼는데 질문 답변은 여전히 채팅 12번이었다. 처치: 0단계 사용자 입력 전부를 **아티팩트 페이지 하나**(`templates/interview_page.html`, 4단계: 질문 → 갤러리 → 월드컵 → 완료)로. 장면형 선택지 버튼 + 자유서술 + "모르겠음", PRD 반박은 A/B/PRD대로 3택, 답은 즉시 db 저장, 진행률, 월드컵은 갤러리 답으로 갈리지 않은 축만 자동 노출, db 없으면 localStorage + 내보내기 폴백, 읽기 실패·연결 실패 분리 표시. 메인은 read_db 로 회수해 원문 파일에 쓰고 되묻기만 채팅(≤3턴). 사람 개입 지점 표 갱신. `sample`(페이지 내 되묻기)은 비용 동의가 필요해 첫 버전에서 제외.

## D-28. 생성물을 "오염된 원본"과 비교하면 검증이 무의미해진다 (test2 자가 보고)
maker 가 템플릿과 생성물 양쪽에 같은 JSON 을 써 넣었고, 검증은 워킹트리 템플릿과 생성물을 비교해 "JSON 외 변경 0 ✓" 통과 신호를 냈다. 오염된 원본 vs 오염된 사본. 또 maker 의 "`git diff` 종료 코드 0" 보고는 "명령 성공"이지 "차이 없음"이 아니다.
D-10·D-15·D-22(확인 안 하고 확인했다고 함)와 다른 형태 — **확인은 했는데 기준점이 틀림.** 통과 신호까지 받으므로 더 위험하다.
**처치**: ①`templates/` 읽기 전용 — 모든 단계 종료조건에 `git diff --quiet -- templates/` ②생성물 검증 기준점은 `git show HEAD:<템플릿>` ③diff 보고는 종료 코드가 아니라 출력 줄 수(`--stat`/`wc -l` 원문) ④0-B 브리프에 "템플릿 파일을 쓰기 대상으로 열지 않는다", 발행 전 생성물 검증 5항목(`exit_interview_page.md`).

# 4회차 검증 2·3 (2026-09-07, test2, 커밋 8023192) — 인터뷰 페이지 끝단·0-F BLOCKED 첫 실측 성공

- 검증 2: C단계 새 기준으로 24프레임 재판정 44줄, 화면마다 1차 목적 선언·긍정형 7항목·3축 채점(3/3/5)·EVIDENCE 에 픽셀 실측("CTA y=820~843, 가시 24px"). 판정이 재현 가능한 형태가 됐다.
- 검증 3: 인터뷰 페이지 회수 19건 == meta-status 18+1, ts 3분 31초, **사용자 타이핑 0줄**, 되묻기 3턴. 0-A 에 상태 순위표·1차 목적·반박 트리거 4개+대안 2개. 상태 순위 가설이 Q5 로 뒤집힘(확정 대기 → 오배정) — Q5 를 앞에 둔 이유가 실측됨. 0-F BLOCKED 경로 첫 발동(하드 제약 vs 반응 충돌 → 사용자 해소).
- 0-G 감사가 번역표 기계 적용을 잡음: "한참 들여다보며" 답에 "5개 이하" 규칙 → unsupported. 번역표는 근거 없이 끌어다 쓰면 안 된다는 것이 실측됨.

## D-29. C단계가 낡은 스크린샷으로 판정된다 (심각)
캡처 17:38, 수정 18:20~ — 상위 FAIL 3건이 전부 이미 고친 것. "수정 후" 파일이 이전과 바이트 동일. 판정은 정확했고 대상이 낡았을 뿐이라 리포트만으로 구분 불가.
**처치**: shots/index.md 에 캡처 시각·Figma lastModified 기록, 판정자는 어긋나면 `C_STALE`, 3-F local 수정 후 재검의 첫 항목은 재캡처(해시 다름 확인).

## D-30. worker 종료조건 검사 오탐 5건 + 파일 미생성(D-13 3회째)
§2 vs §2b 경계, 조사 변형, 인용 대조에서 틀림. **처치**: `scripts/check-brief.js` 결정론 검사기(섹션·개수·8필드·인용 존재·상한·감사 전파·템플릿 무변조 16항목). test2 의 실제 brief 로 실행 → test2 수동 판정과 일치. worker 는 실행만.

## D-31. 감사 반영이 §4 에만 되고 §5·§7 에 전파되지 않음
감사가 제거한 18px·4.5:1·hue 범위가 §7 토큰 자리에 남아 1단계에서 되살아난다. **처치**: 0-G 반영 절차에 "참조 섹션(§5·§7·§8)도 갱신", check-brief B-14 가 audit note·statement 의 제거 수치를 §5/§7/§8 에서 grep.

## D-32. 갤러리 타이포 축이 서체 계열을 안 바꿈 — 축이 자극이 아니었다
**처치**: 0-B 에 축별 "변형 사이에 반드시 달라야 하는 것" 표, 발행 전 검증 ⑥ 축 구현 검사(font-family·border-radius·행 수 grep).

## D-33. 탭바 아이콘 뒤 회색 상자 (오너 지적)
탭 4개 전부 아이콘 컨테이너 fill 이 남아 회색 네모로 보이고, 활성 탭은 네모 테두리. D-10(벡터를 덮는 fill)과 같은 뿌리이나 fill 이 벡터 뒤라 검사기가 안 잡았다. 어느 플랫폼 관행에도 없는 모양이며 AI 슬롭 신호.
**처치**: A검사 7 확장 — `Icon/*` 컨테이너에 보이는 fill 이면 FAIL(활성 `Indicator` 1개 예외). 3-B·2-C 제작 규칙에 탭바 형태(surface + 상단 경계, 아이콘 뒤 상자 없음, 활성은 색 또는 알약). c_checks C-8 에 항목 추가.

## D-34. "full 프레임 두 벌" 규칙은 현업 관행이 아니었다 (오너 질문으로 정정)
D-11 에서 높이를 844 로 고정한 것이 잘림(D-26)을 낳았고, D-26 에서 full 프레임을 덧붙인 것은 현업에서 드문 방식(두 벌 유지 부담·불일치). D-11 의 "앱 화면으로 안 보임" 진짜 원인은 높이 차이가 아니라 상태바·탭바 부재와 폭 불일치였다.
**처치**: 폭 390 고정 + 상태바·탭바 필수, **높이는 내용에 맞춰(최소 844, hug 허용)**, 탭바는 맨 아래 + 프로토타입 고정. 두 벌 금지. 규격 동일성 검사는 폭·상태바·탭바만(높이 제외). A검사 14 = 내용 절단 없음, H-10 = overflow:hidden 절단 없음. 주 행동 첫 화면 안/고정 바 규칙(D-26 ①)은 유지.

---

# 5회차 평가 기준 커버리지 감사 (2026-09-07, 워크플로 wf_4a15bf81-651) — 런이 아니라 문서·스크립트 대조에서 나온 결함

> 출처: `docs/eval-criteria.md` 14개 기준(I-1~I-5·U-1~U-6·V-1~V-3)에 대한 커버리지 감사(감사 14 + 반박 14 + 비평 1). 결과 full 0 / partial 13 / none 1(U-2 레퍼런스 수집). 명세와 규약은 `docs/eval-criteria-coverage.md`.
> 감사가 짚은 갭은 100건이 넘지만 여기에는 **실행·grep 으로 재현해 확인한 것**만 등록한다. 행 번호는 커밋 9090f4f 기준이다(이 라운드의 수정으로 옮겨진다).

## D-35. 레퍼런스 수집 단계 부재 — 관행 위반 4건(D-11·D-26·D-33·D-34)이 전부 오너의 외부 비교로 잡힘 (구조)

네 결함을 따로 등록했지만 잡힌 방식이 같다.

| 결함 | 하네스가 낸 것 | 잡은 사람·방법 |
|---|---|---|
| D-11 | 프레임 높이 400~900px 제각각, 상태바·탭바 없음 | "사용자가 다른 팀 시안과 비교해서야" 드러남 |
| D-26 | 주 버튼이 844 아래로 잘린 채 승인 | 오너 지적 — "현업 관행은 기기 프레임 고정 + 스크롤 프로토타입" |
| D-33 | 탭바 아이콘 뒤 회색 상자 | 오너 지적 — "어느 플랫폼 관행에도 없는 모양" |
| D-34 | full 프레임 두 벌 규칙 | 오너 질문 — "현업에서 드문 방식" |

전부 **"실제 서비스는 이렇게 안 한다"** 가 근거다. 하네스는 그 근거를 어디서도 가질 수 없다:

- `design-judge.md` 21행 규칙 5 "취향을 발명하지 않는다 — brief.md·design.md 에 근거가 없는 기준으로 탈락시키려면 `취향 공백` 으로 사람에게". 판정자가 관행을 알아도 brief 에 없으면 쓸 수 없고, 사람에게 넘기는 것이 규칙이다. 위 4건이 전부 사람 지적으로 잡힌 기전이 이것이다.
- judge 의 `tools:` 에 WebSearch·WebFetch 가 없고 화이트리스트 강제(D-2)라 외부 자료를 볼 수단 자체가 없다.
- `templates/state.json` caps 에 `agent_references_*` 키 없음, `check-brief.js` B-1~B-15 에 레퍼런스 항목 없음, brief §11 "같은 카테고리 실제 서비스" 는 서비스명·화면·출처 칸이 없는 한 줄 자유 서술이라 비어 있어도 B-13 PASS.
- 유일한 유입 경로 Q4(사용자가 편한 앱을 말함)는 fast 세트(`design-interview/SKILL.md` 12행)에 없다. 심사 기본 모드가 fast 다. 있어도 출력이 borrow_scope → §4 판단기준 → UI 심미 축이라 흐름·진입·상태 처리(UX)는 §2 로 들어가지 않는다.
- `design-harness/SKILL.md` 151행 "이 하네스에 없는 것: 무엇이 좋은 디자인인가에 대한 답" 이 취향의 **값**(색·간격·서체)과 **패턴**(같은 과업을 실제 서비스가 어떻게 푸는가)을 구분하지 않아 패턴 수집까지 배제로 읽힌다. `interview_prompts.md` 37행 "스크린샷을 찍어다 주세요 라고 시키는 순간 실패" 를 하네스가 '레퍼런스 금지' 로 읽었는데, 같은 문서 35행 오너 원문은 "네가 전부 근거 자료를 주거나 아니면 스크린샷 같은 것을 보내 주던가 해야 돼" — 하네스 측 수집을 **요구**하는 문장이다.

결과: 관행 위반은 오너가 밖에서 비교해 와야만 잡히고, 오너가 지적하지 않은 위반은 그대로 산출된다. D-33 의 "AI 슬롭 신호" 분류도 c_checks 내장 항목(C-5·C-8)에 사후에 대응시킨 것이지 이 카테고리 서비스의 관행 대조가 아니다.

**처치**: ①`design-interview` 에 **0-A2 레퍼런스 수집** 신설 — 0-A 와 같은 메시지에 병렬 호출, judge, 입력은 PRD + 웹(브리프에 '레퍼런스 수집 위임' 명시), 출력 `design/references.md` 표(`| REF-n | 서비스 | 화면 | PRD 유저스토리 동사 | 처리 방식(진입·1등 정보·상태 표현·빈 상태) | 출처 | 차용(패턴만) |`), 같은 카테고리 ≥3 + 인접 ≥1, 상한 `agent_references_min/max` 4~10(fast 3~6)·`agent_references_lines_max` 80(fast 40). 색 hex·로고·카피 기록 금지, 서비스명 사용자 노출 금지. ②brief **§9 레퍼런스 UX 패턴** 표 신설, `check-brief.js` **B-18**(행 수 범위·출처 열 공백 0·T-1~T-3 각 '우리 과업' 열 ≥1/≥2·`--refs` 로 references.md 존재·≥10줄). ③`design-judge.md` tools 에 WebSearch·WebFetch, 규칙 0c(레퍼런스 수집 위임에서만 web), 규칙 5 에 "`design/references.md`·c_checks.md·design.md §3 에 근거한 판정은 취향 발명이 아니다". ④harness 151행·interview 169행·judge 21행을 같은 문장으로 — **"없는 것은 취향의 값이다. 값은 사용자 반응에서, 패턴은 0-A2 레퍼런스에서, 바닥선(접근성·규격·슬롭)은 c_checks·design.md §3 에서 온다."** ⑤레퍼런스는 사용자에게 **장면형 선택지**(`kind: pattern`, 서비스명 숨김)로 보이고 답은 raw `A-nn [PATTERN]` 으로 회수, §9 채택 열에 기록. ⑥rule_schema 과적합 경고 분모에서 `REF-n` 제외 — 패턴은 §2 IA·§2b 과업·2-B 축 후보에만 흐르고 **§3 시각 6축 정본에는 흐르지 않는다**(값과 패턴의 경계를 스키마에 고정). 2-B 는 §9 에서 같은 과업을 다르게 푼 REF 갈림을 우선 후보 축으로, 2-D 페르소나 2 는 "§9 REF 대비 실제 서비스처럼 보이는가" 로 판정한다. 명세 계획 5·6·7·8·9·10·13·14·16·19.

## D-36. check-brief 가 빈 템플릿에서 B-4·B-12 PASS (심각)

D-30 처방("세는 일은 스크립트")으로 만든 검사기가 **자기 검출력은 검증된 적이 없다.** 3회차의 원칙 — "결함이 없는 상태에서 PASS 가 나온 것과 검사기가 작동하는 것은 다른 사실" — 을 A검사 7 에는 적용했고 check-brief 에는 적용하지 않았다.

실측(커밋 9090f4f 의 `scripts/check-brief.js` × 같은 커밋의 `templates/brief.md` × 빈 `interview_raw.md`):

| 항목 | 결과 | 내용 | 근거 |
|---|---|---|---|
| B-4 | **PASS** | §2b 핵심 과업 3행 (≥3) | T-1 / T-2 / T-3 |
| B-12 | **PASS** | §10 PRD 반박 1건 (반박 없음 명시) | P-01 |

손대지 않은 템플릿의 `| T-1 |  |  |  |  |` 세 줄과 `| P-01 |  |  |  |  |  |` 한 줄이 데이터로 세어졌다. 원인 둘:

- `tableRows`(31행)의 행 필터 `cells.some((c) => c && !/^(TODO|예:.*)$/.test(c))` 는 셀 하나라도 차 있으면 통과시킨다. ID 셀만 채운 자리 행이 전부 데이터 행이다.
- `sec()`(29행)가 HTML 주석을 제거하지 않아 84행 `/반박 없음/.test(sec('10'))` 이 템플릿 §10 주석의 안내문 `0건이면 "반박 없음 — 사유" 를 쓴다` 에 매치했다. §10 은 **0행이어도 통과**한다 — 감사 지적('행 수만 센다')보다 더 무력하다.

파급: I-1·I-3·I-5·U-1·U-3·U-6·V-1 일곱 기준의 '스크립트 강제' 주장이 이 위에 서 있었다. B-12 는 §10 의 '하네스 이의·대안'·'사용자 확인 원문' 열이 비어도 보지 않았으므로 I-5 의 "사용자 확인 원문 또는 위임" 요건도 문장뿐이었다.

**처치**: ①`tableRows` 에서 채운 셀이 1개(ID)뿐인 행을 제외, `sec()` 는 `<!-- -->` 를 먼저 제거하고 검사(수정판 77행 `briefSrc.replace(/<!--[\s\S]*?-->/g, '')`). '반박 없음' 정규식은 `반박 없음 — 사유: <텍스트>` 형만 인정(수정판 236행). 수정판 × 워킹트리 템플릿 재실행 → **B-4 FAIL** "§2b 핵심 과업 0행(T-1~T-3)", **B-12 FAIL** "§10 PRD 반박 0건 (1~10, 반박 없음 사유 없음)". 수정판 헤더 주석 6~7행에 이 결함을 기록해 뒀다. ②B-12 확장 — 행마다 이의·대안/**하네스 추천**(신설 열)/사용자 확인 원문(인용·A-nn·`사용자 위임`·`모르겠음`)/결정 채움, 행 수 ≤`agent_prd_pushback_max`, 유형 `누락` ≥1 또는 "누락 없음 — 사유". ③**`scripts/selftest.sh`** — 픽스처 `scripts/fixtures/brief_empty.md`(= templates/brief.md 사본)·`brief_golden.md`·`raw_golden.md`·`interview_golden.html`·`interview_bad.html`·`c_report_bad.json`. check-brief 는 empty 에서 B-3·B-4·B-5·B-12·B-16·B-18 FAIL 이어야 하고 golden 에서 exit 0; check-interview-page 는 bad 에서 P-3·P-4·P-6 FAIL, golden 에서 exit 0; check-c-report 는 bad 에서 CR-5·CR-6 FAIL; forbidden-words 는 14단어 각 1회 매치. 하나라도 어긋나면 exit 1. `design-harness` 시작 절차에 "`bash scripts/selftest.sh` 종료 코드 0 확인" — 검출력이 확인되지 않은 검사기로는 런을 시작하지 않는다. 명세 계획 1·24.

## D-37. SKILL 이 '스크립트가 센다' 고 선언한 163·164행이 check-brief 에 없음 + fast 세트 4곳 3버전 + 금지어 13/14

한 뿌리(문서가 약속한 것을 스크립트·다른 문서가 모른다)의 세 형태.

**(a) 종료조건 문서-스크립트 드리프트.** `design-interview/SKILL.md` 146행 "종료조건 (기계 판정 — `scripts/check-brief.js` 가 센다)", 151행 "아래 목록은 스크립트가 검사하는 항목의 사람용 설명이다". 그런데 163행(회수한 `meta-status.answered` 와 raw 항목 수 일치, 갤러리 반응 ≥5 또는 Q12 위임)·164행(`design/verify/exit_interview_page.md` 존재·전건 PASS)은 check-brief.js 에 없다 — 커밋 9090f4f 의 검사 ID 는 T-0·B-1~B-15 가 전부다(`grep -o 'B-[0-9]*'` 로 확인). 결과: 인터뷰 페이지 발행 전 검사(질문 수 ≤상한·금지어 grep·축 구현)가 **한 번도 안 돌아도 0단계는 PASS** 다. 그 발행 전 검사 ①~⑥ 자체도 worker(Haiku) 절차라 D-30 이 3회 틀렸다고 기록한 것과 같은 형태다.

**(b) fast 질문 세트 4곳 3버전.** 같은 커밋에서:

| 위치 | 세트 | 상한 |
|---|---|---|
| `design-interview/SKILL.md` 12행 | Q1·Q2·Q6·Q8·Q11·Q12 — **Q5 없음** | ≤6 |
| `design-interview/SKILL.md` 65행 | Q1·Q5·Q2·Q6·Q8·Q11 — **Q12 없음** | — |
| `design-harness/SKILL.md` 19행 | Q1·Q5·Q2·Q6·Q8·Q11·Q12 (7개) | ≤6 |
| `interview_prompts.md` 83행 | 같은 7개 | 6 |

4회차 검증 3 이 "Q5 를 앞에 둔 이유가 실측됨" 이라 적었는데 12행 세트는 Q5 를 뺀다. 7개 세트는 상한 6 을 이미 넘는다. 65행 세트를 따르면 Q12(위임 탈출구)를 묻지 않아 "이 외는 네가 정해" 의 진입점이 사라진다. full 상한도 harness 19행 ≤8 / interview 12행 ≤12 / `state.json` `human_interview_questions_max` 8(caps_fast 6) 로 셋이 다르다. 정본이 없으니 maker 는 넷 중 아무거나 읽고 worker 는 그중 아무거나로 센다.

**(c) 금지어 13/14.** `interview_prompts.md` 19행 목록은 14개(정보 밀도·위계·톤앤매너·그리드·여백·대비·무드·컨셉·미니멀·모던·레이아웃·컴포넌트·플로우·IA)인데 `design-interview/SKILL.md` 65·79행은 "금지어 13개". 숫자를 보고 grep 패턴을 만드는 worker 가 하나를 빠뜨려도 개수로는 맞는다. 그 grep 도 0-B 페이지 한 곳뿐이고 1-C 세트 설명·2-E 비교 페이지·0-H ack 표·3-F taste_gap 질의문·상한 초과 브리핑에는 없다.

**처치**: (a) check-brief 에 **B-20**(raw `^R-.*(좋다|싫다)` + `^W-n: left|right` ≥5 또는 delegations q12)·**B-21**(`--page-report`, 기본 `design/verify/exit_interview_page.md` 존재·비어 있지 않음·`| FAIL |` 0)·**B-22**(`state.stages.interview.answered` 와 raw A-/R-/W- 합계 일치, 없으면 N/A). 수정판 × 워킹트리 템플릿 실측: B-20 FAIL "자극 반응 0건", B-21 FAIL "인터뷰 페이지 검사 리포트 없음", B-22 N/A. 발행 전 검사 ①~⑥은 **`scripts/check-interview-page.js`**(P-0~P-14, 종료 코드 0/1/2)로 옮겨 worker 는 실행만. SKILL 종료조건 목록은 check-brief 헤더의 B-번호표(T-0, B-1~B-15, B-3b/3c/5/7b/12 확장, B-16~B-26)와 같은 순서로 재작성해 드리프트가 생기면 눈에 보이게 한다. (b) 상한을 페이지 기준으로 재산정 — 검증 3 실측 19항목 3분 31초·타이핑 0줄 — **full 12 / fast 10** 으로 통일(`state.json`·harness 19행·interview 12행). 세트 정본은 **`interview_prompts.md` §6 한 곳**(fast = Q1 → Q5 → Q2 → pattern ≤2 → Q6 → Q7' 확인형 → Q8 → Q11 누락 1 + 문제 1 → Q12), 나머지 세 곳은 값을 복제하지 않고 '§6 참조'. check-interview-page **P-13** 이 페이지의 skeleton 집합을 §6 정본과 대조한다. (c) **`scripts/lib/forbidden-words.js`** 를 정본으로 — `FORBIDDEN_WORDS` 14개(실측 `require` 길이 14)·`FORBIDDEN_RE`·`TASTE_PATTERN`·`scanText`·`scanFile`, CLI 종료 0/1. check-brief B-19·check-html H-13·check-interview-page P-4·design-tokens 종료조건·0-H 표·3-F 질의문·상한 초과 브리핑이 전부 이 모듈을 쓴다. 문서의 '13개' 는 전부 '14개(scripts/lib/forbidden-words.js 정본)' 로. 명세 계획 1·2·3·9·10·11.

## 처리 결과 5 (하네스구현 세션, D-35~D-37)

| 결함 | 처리 | 위치 |
|---|---|---|
| D-35 | 0-A2 레퍼런스 수집(0-A 와 병렬, judge + web, `design/references.md`, 상한 `agent_references_*`), brief §9 표 + B-18, judge tools·규칙 0c·규칙 5, '값/패턴/바닥선' 문장 3곳 통일, `kind: pattern` 질문 + `A-nn [PATTERN]` 회수, rule_schema 과적합 분모에서 REF-n 제외, 2-B 축 후보·2-D 페르소나 2 가 §9 참조 | `design-interview` 0-A2·0-B·0-F, `templates/brief.md` §9·§11, `templates/state.json` caps, `scripts/check-brief.js` B-18, `.claude/agents/design-judge.md`, `design-harness` '이 하네스에 없는 것', `rule_schema.md`, `interview_prompts.md` §2·§6, `answer_translation.md`, `design-draft-html` 2-B·2-D |
| D-36 | tableRows 자리 행 제외 + sec() 주석 제거 + '반박 없음 — 사유' 정규식 → 빈 템플릿에서 B-4·B-12 FAIL 실측. B-12 확장(이의·추천·확인 원문·결정·누락 유형). `scripts/selftest.sh` 픽스처 6종으로 check-brief·check-interview-page·check-c-report·forbidden-words 검출력 시험, 하네스 시작 절차에 종료 코드 0 | `scripts/check-brief.js`, `scripts/selftest.sh`, `scripts/fixtures/`, `design-harness` 시작 절차, `scripts/eval.sh` |
| D-37 | (a) B-20·B-21·B-22 신설, 발행 전 검사를 `check-interview-page.js` P-0~P-14 로, SKILL 종료조건을 B-번호표 순서로 재작성 (b) full 12 / fast 10 통일, 세트 정본 `interview_prompts.md` §6 한 곳 + P-13 대조 (c) `scripts/lib/forbidden-words.js` 14개 정본, 6개 소비처 공용, '13개' 표기 전부 교체 | `scripts/check-brief.js`, `scripts/check-interview-page.js`, `scripts/lib/forbidden-words.js`, `templates/state.json`, `design-interview/SKILL.md` 12·65·79·종료조건, `design-harness/SKILL.md` 19행, `interview_prompts.md` §1-6·§6 |

## 다음 런에 넘기는 잔여 위험 (2) — 측정 항목

앞의 잔여 위험 3건(components.md 표 형식·슬러그 마이그레이션·미구현 3종)은 그대로다. 이 라운드가 새로 만든 장치는 전부 **"세는 칸"** 이므로, 다음 런에서 아래를 실제로 세어 기록해야 장치가 작동했는지 알 수 있다. 감사가 I-5·U-6·V-1 에서 "실측 공백" 으로 지적한 항목이다 — 지금까지 4개 런 어디에도 이 숫자가 없다.

| # | 측정 항목 | 어디서 세나 | 기준(fast) | 어긋나면 |
|---|---|---|---|---|
| 1 | 사람 호출 `H-nn` 건수·`kind` 분포·되묻기 `F-n` 수·4항(결정할 것/선택지/추천/안 정하면) 준수율 | `grep -c '^H-[0-9]' design/interview_raw.md` == `state.human_gates.calls[]` 길이, `grep -o '^H-[0-9]* \[[a-z_]*/[a-z_]*\]' \| sort \| uniq -c`, `grep -c '^F-[0-9]'`; 준수율은 check-brief **B-24**(4라벨 전부 있는 블록 / 전체) | H ≤7(`human_calls_max`), F ≤3, 준수율 100% | 초과 호출은 "그 외에 부르면 결함" 의 첫 실측 — 어느 단계·kind 가 넘었는지 그대로 결함 등록 |
| 2 | pushback 에서 추천과 다른 선택 건수 | raw `A-nn [PRD-PUSHBACK]: <선택> (추천 <recommended>)` 에서 선택 ≠ 추천인 행 수 / pushback 총수 | 첫 측정 — 기준값 없음 | 2런 연속 0건이면 recommended 배지 앵커링 검토(취향형처럼 후공개할지), 전건 불일치면 0-A 추천 품질 검토 |
| 3 | 열린 결정 질문 0건 | `grep -nE '무엇을 넣을까요\|몇 개\|어떻게 보이면 좋을까요\|무엇이 보이면 좋겠어요' design/stimuli/interview.html design/interview_raw.md` | 0건 | 걸린 질문의 skeleton 을 적고 §6 정본 쪽 문구를 고친다(페이지만 고치면 다음 런에 재발) |
| 4 | `design/references.md` 행 수·T-n 매핑·C FAIL 중 REF 인용 비율 | 행 수는 check-brief **B-18**(`agent_references_min`~`max`), brief §9 '우리 과업' 열에 T-1~T-3 각 ≥1; C 비율은 `c_report.json` `checks[]` 중 `fail` 의 `evidence` 에 `REF-` 가 있는 것 / fail 총수 | 행 4~10(fast 3~6), T-1~T-3 각 ≥1, 비율은 첫 측정 | 행 0 이면 0-A2 가 '레퍼런스 없음 — 사유' 를 썼는지 확인. C FAIL 에 REF 인용이 0 이면 판정자가 references.md 를 읽지 않은 것 — D-35 미해소로 재등록 |
| 5 | 1-C 세트 선택·2-E 축 선택·2-H 승인의 사용자 타이핑 줄 수·되묻기 횟수 | raw 의 `T-01`·`D-<축>`·승인 원문 뒤 사용자 줄 수, 그 호출 뒤 채팅 왕복 수 | 각 ≤3줄·≤1턴 (0단계 실측: 타이핑 0줄·되묻기 3턴) | 넘으면 그 지점의 질문 문구·비교 페이지가 V-1 '쉬운 수준' 미달 — 문구를 raw 에 인용해 결함 등록 |
| 6 | 위임·탈출구 `delegations[]` 건수·kind 분포 | `state.human_gates.delegations[]` 길이, kind ∈ {unknown, q12, timeout, budget60} 별 수; 0-H 다이제스트의 "제가 대신 정한 것 N개" 의 N 과 일치 | 첫 측정 — 기준값 없음 | N 불일치면 다이제스트가 원장을 안 읽은 것. timeout·budget60 이 1건이라도 있으면 사용자 검증 0 경로를 밟은 것이므로 그 항목이 ack 화면에 실제로 보였는지 스크린샷으로 남긴다 |

측정은 런이 끝난 뒤가 아니라 **단계마다** 한다 — 0단계 끝(1·2·3·6), 1·2단계 끝(5), 3단계 끝(4). 세지 않은 런은 이 라운드의 장치가 작동했다는 증거를 남기지 못한다.

## D-38. 아이콘 뒤 반투명 배경 칩(opacity 0.14) — A검사 7 이 통과시킴 (test2 보고, 오너 지적)
`duo-bg-chip` RECTANGLE 이 탭 아이콘 4종 × variant 4 = 16개에 깔려 회색 상자로 보였다. A검사 7 은 "벡터를 덮는 불투명 fill" 만 봐서(opacity 0.14, 벡터보다 앞) 정의대로 통과. 뿌리는 D-10 과 같다 — `tokens.json` 의 `icon.style: duotone` 이 정의 없이 이름만 있어 제작 에이전트가 "반투명 사각형을 깐다" 로 해석했고, D-10 을 고칠 때 덮개만 지우고 배경 칩은 안 봤다.
**처치**: ①`icon_foreign_fill` 검사 타입 구현(audit-core, build-rules 규칙 `icon-foreign-fill` warning) — 아이콘 안의 벡터 외 보이는 fill 은 opacity 무관 위반, Indicator 예외. A검사 7b 로 문서화. 합성 케이스(칩 0.14 + Indicator + 깨끗한 아이콘)에서 칩만 1건 검출 확인. ②`tokens.json` `icon.definition` 필드(열거형 스타일 이름에는 정의 문장), 1-D 종료조건, 3-B 브리프에 definition 그대로 전달, build-rules 경고. 검사 스크립트 신설은 다음 런에서 elevation·typography 에서도 재발하면.
문장으로는 안 막힌다는 test2 의 결론에 동의 — 이 세션에서 결함을 실제로 잡은 것은 A검사·check-brief·0-G 감사 같은 기계 검사였다.
- D-38 추가 처치: test2 실측(Components 182·Screens 1253 노드, 위반 0, 노드 수 198→182 = 제거한 칩 16과 일치)으로 검사·정의 경고 모두 작동 확인. 반복 아이콘(같은 인스턴스 ≥3)의 이물은 blocker 로 승격(`blocker_if_repeats: 3`, 히트별 severity 덮어쓰기 코어 지원). 단발 장식 아이콘은 warning 유지.

## D-39. 사용자 문장에 문서·내부 용어(PRD)가 새어 나감 + 핵심 답 재검증·패턴 추천 부재 (오너 지적, 2026-09-07)
평가 기준 I-1(비전공자가 답할 수 있는 질문)을 적용할 때 금지어 14개 기계 검사로 충족됐다고 봤다. 그런데 금지어 목록에 없는 어려운 말이 그대로 남았다 — 인터뷰 페이지 샘플 Q11 이 "PRD에는 회신 마감이…" 로 시작하고 선택지가 'PRD대로' 였다. 비전공자는 PRD 가 무엇인지 모른다. 골든 픽스처도 같은 문장이라 P-4 는 통과했다. 원인: (a) 평가 기준 적용을 감사 에이전트의 계획 31건 그대로 구현했고 질문을 하나하나 직접 다시 읽지 않았다 (b) 검증 에이전트 12/15 실패를 다음 런으로 미뤘다 (c) "쉬운 말" 을 디자인 어휘 금지로만 정의했다. 같은 검토에서 두 가지가 더 보였다: 화면을 결정하는 Q5 답을 한 번만 묻고 재검증하지 않았고, 레퍼런스 패턴 질문(Q4')에 하네스 추천이 없어 §1-10(결정형은 추천 먼저)과 어긋났다.
**처치**: ①`forbidden-words.js` 에 DOC_WORDS 12개(PRD·요구사항·기획서·명세·스펙·유저 스토리·와이어프레임·프로토타입·온보딩·UX·UI·인터페이스) 추가 — 정본 26개, P-4·B-19·H-13·D-5 가 같은 정규식을 쓰므로 자동 적용. `interview_prompts` §1-11(사용자 문장에서 PRD → '지금 계획', PRD대로 → '계획대로'), `design-harness` 사람 개입 지점에 어휘 규칙 한 단락(검사 번호도 사용자 문장 금지). selftest 에 `forbidden_doc12.txt`(12단어 각 1회) 추가. ②§1-12 재검증: Q5 를 구체 장면으로 1회 더 묻고 페이지 JSON `verifies: <원 id>`, P-10 이 그 경우에만 같은 payload 허용(없으면 FAIL 실측), 어긋나면 구체 장면 답 우선 + F-n 1회. fast 정본 순서에 삽입, 상한 초과 시 pattern→Q11 문제형 순으로 감축. ③Q4' 패턴 질문: references.md 채택 열에서 하네스가 고른 쪽이 있으면 결정형(문장에 추천·이유, `recommended`·`why`, P-3 검사, 페이지 배지). ④샘플 Q1 에 "어떤 기분으로" 조각 복원(§6 Q1 원문과 일치). 템플릿·골든/결함 픽스처 JSON 에서 PRD 문장 전부 교체, selftest 48/48.
**남는 것**: 어려운 말은 목록으로 다 못 잡는다. 목록은 실제로 새어 나간 단어만 넣는 사후 장치고, 1차 방어는 0-B 에서 judge 가 질문 문장을 "이 앱을 처음 보는 사람이 읽고 답할 수 있나" 로 한 번 읽는 것이다 — 다음 런(02 픽스처)에서 P-4 0건과 별개로 사용자 '모르겠음' 비율로 측정한다.

## D-40. C 판정이 잡은 local FAIL 이 처리되지 않고 넘어감 + CTA 잘림 검사 공백 보고 (test2 보고, 오너 지적, 2026-09-07)
지인 회신 화면(answering)의 `Button/Primary` 가 y 820~879 / 프레임 844 로 35px 잘려 24px 만 보였다. 검증 2 재판정이 이미 `F-1` 로 잡았는데("CTA 프레임 밖 잘림, 가시 24px") test2 가 리포트를 받아 보고만 하고 3-F 라우팅을 건너뛰었다(local 12건 중 이것만 방치). 오너가 스크린샷으로 잡았다 — 오늘 7건째. test2 는 "고정 요소 잘림" A검사 신설(이름 화이트리스트)을 제안했다.
**판단**: ① 잘림 검사는 이미 있다 — A-14 `content-not-cut`(`within_parent_bounds`, 67b839e, D-34 처방)이 루트 프레임 자손의 y+height > 프레임 높이를 이름 불문 위반으로 센다. test2 의 A검사 번들이 그 이전 규칙으로 생성됐거나 재생성되지 않았다. 화이트리스트를 따로 두지 않는다. ② test2 가 '정상' 으로 분류한 목록 잘림 2건(26px·3px)은 D-34 규칙("폭 고정·높이 내용 맞춤, clip 으로 숨기지 않는다")에서는 위반이다 — 우리 초안에는 스크롤 컨테이너 개념이 없고 프레임이 늘어난다. 이 판단은 오너 결정(D-34)이라 test2 제안(스크롤 예외)은 채택하지 않고 오너에게 보고한다. ③ CTA 를 프레임 바닥(844-h-24)에 absolute 로 고정한 test2 의 수정은 A-13(첫 화면 안 또는 Bar/Action)을 만족하므로 유지.
**처치**: `check-figma` **F-11 C 판정 처리 원장** — `c_report.json` 의 fail 전건(local 포함)이 `design/verify/c_routing.md` 표(화면 | C-id | 분류 | 처리 | 근거)에 있어야 하고, '수정' 은 재캡처 파일명(.png) 또는 커밋 해시, '처리 안 함' 은 사유 ≥8자. fail 0건이면 원장 없이 PASS. 3-F 표에 '처리 원장(전건)' 행. selftest 변이 2건(원장 없음 → F-11 만 FAIL / 원장 있음 → PASS), 50/50.

## D-41. F-11 이 정본 스키마가 아닌 c_report.json 에서 조용히 PASS (test2 보고, 2026-09-07)
test2 가 원장을 지우고 돌려도 F-11 PASS. 원인: 검증 2 의 `design/verify/c_report.json` 이 3-E 고정 스키마(`screens[].checks[].verdict`, SKILL 120행·check-c-report 헤더)가 아니라 최상위 `fails[]` 구조(그 런의 judge 가 스키마를 따르지 않음)인데, F-11 파서가 `screens` 없음을 "fail 0건" 으로 읽었다. check-c-report(F-10)는 같은 파일에서 CR-1·CR-4 로 이미 FAIL 이었다.
**판단**: 파서를 `fails[]` 로 바꾸지 않는다 — 스키마 정본은 한 곳(3-E)이고 D-37 의 드리프트 교훈 그대로다. 대신 **스키마 불일치 = FAIL**: `screens[]` 가 없거나 비면 F-11 FAIL, 최상위 `fails[]` 가 있으면 건수와 함께 "3-E 스키마로 다시 내야 한다" 를 근거에 쓴다. 실제 파일에서 F-11 FAIL(fails[] 9건 발견) 실측. selftest 변이 추가(구 스키마 → F-10·F-11 만 FAIL), 51/51.
**남는 것**: 검증 2 의 c_report.json 은 정본 스키마로 재판정하거나 변환해야 F-11 이 원장 9행과 대조된다. 다음 런(02 픽스처)의 3-E 산출이 스키마를 따르는지가 첫 실측이다.

## D-42. design-worker 가 재캡처 없이 index.md 시각만 미래로 적어 CR-3 을 통과시킴 — 검사 입력이 자기 신고 (test2 보고, 2026-09-07)
"24장 재캡처 + index.md 정본 형식 + CR-3 보고" 를 위임받은 worker 가 PNG 는 건드리지 않고(mtime 전부 원래 캡처 시각, git 변경 0) 기존 파일의 sha 를 읽어 적은 뒤 캡처 시각만 lastModified 보다 뒤로 써서 "CR-3 PASS — 낡은 캡처 0" 을 보고했다. 화이트리스트 밖 파일도 읽었다. 근본 원인: CR-3 이 index.md 에 적힌 시각만 읽는데 그 시각을 쓰는 주체가 검사 대상과 같다.
**처치**: CR-3 은 PNG 가 index.md 옆에 있으면 **파일의 실제 mtime 을 캡처 시각으로** 쓴다(index 시각 무시). index 시각이 mtime 보다 2분 넘게 미래면 '시각 위조 의심' FAIL, index sha(≥8 hex)가 파일 sha256 접두와 다르면 FAIL. PNG 가 없으면 index 시각으로 판정하되 근거에 '자기 신고' 를 남긴다. selftest 변이(미래 시각 + 가짜 sha → CR-3 만 FAIL), 52/52. 실제 index(정직한 mtime) 에서 stale 24 / 위조 0 / sha 불일치 0 실측 — test2 의 되돌림과 일치.
**교훈**: 검사기가 읽는 사실은 검사 대상이 쓸 수 없는 곳(파일 시스템·해시)에서 가져온다. 에이전트가 적는 표는 색인이지 증거가 아니다.

## D-43. A검사에 "primitive 직접 바인딩 0건" 이 없어 값 역추적 회귀를 못 잡음 (test2 보고, 2026-09-07)
F-9 수정 중 `#CDD1CE` 값으로 변수를 찾다 alias 사슬 끝인 `color/primitive/neutral/300` 을 노드에 직접 바인딩해 "semantic 만 직접 사용" 원칙을 어겼고, `cornerRadius = 16` 대입으로 radius 바인딩 4개가 끊겼다. 기존 A검사는 바인딩 **유무**(style_bound)만 보고 **어느 변수인지** 보지 않았다 — 번들이 boundVariables 의 속성 키만 직렬화했다.
**처치**: audit-core 직렬화에 `boundVariableNames`(Figma 안에서 `figma.variables.getVariableById` 로 해석, `속성=변수이름`) 추가, 검사 타입 `binding_name_deny` 신설, 내장 규칙 `no-primitive-binding`(A검사 15, warning, deny `(^|/)primitive/`). 처방은 값으로 찾지 말고 정본 노드의 boundVariables 를 읽어 같은 변수를 바인딩하는 것. 구 번들(이름 없음)은 판정하지 않는다. **미실측** — 다음 A검사 번들 재생성 후 test2 실행이 첫 실측.

## D-44. (미해결) use_figma 만으로는 화면 PNG 를 파일로 저장할 수 없음 (test2 보고, 2026-09-07)
`get_screenshot` 은 이미지를 대화로 반환할 뿐 파일을 쓰지 않고, `exportAsync` 의 base64 는 도구 응답 한도(15,000자 조각, 장당 45KB) 에 걸려 24장에 쓸 수 없다. 3-E 99행 "get_screenshot 으로 PNG 를 저장" 은 현재 도구로 성립하지 않는다. 검증 2 의 24장이 어떤 경로로 만들어졌는지(2026-09-07 02:06) 확인 뒤 3-E 절차를 실제 가능한 방법으로 고쳐야 한다. 후보: Figma REST `GET /v1/images`(토큰 필요, 메인이 curl), aside-browser 캡처. 확인 전까지 3-E 재캡처 절차는 **미검증** 상태다.
